# 작업 내역

- **날짜**: 2026-06-15 15:45
- **작업자**: Claude

## 작업 내용

YouTube 실제 업로드 어댑터를 구현하고 Mock 모드에서 Real 모드로 전환하였다.

## 변경된 파일

- `backend/app/services/youtube.py`: 실 업로드 구현 (`_build_client`, `_real_upload` 함수 추가)
- `backend/app/routers/youtube.py`: `upload_video` 호출 시 `video.output_path` 전달, `/connect` 엔드포인트 모드 반영
- `backend/requirements.txt`: `google-api-python-client`, `google-auth-oauthlib`, `google-auth-httplib2` 추가
- `.env`: `YOUTUBE_UPLOAD_MODE=mock` → `real` 변경

## 주요 구현 내용

- `google.oauth2.credentials.Credentials`로 refresh_token 기반 인증
- `MediaFileUpload` resumable 청크 업로드 (4MB chunk)
- 예약 발행 시 `publishAt` 필드 설정 및 `privacyStatus=private` 강제 적용 (YouTube 정책)
- 파일 미존재 시 명확한 RuntimeError 반환
