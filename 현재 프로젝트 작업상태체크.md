# 현재 프로젝트 작업상태체크

> 기준일: 2026-06-12  
> 기준 문서: 프로젝트_작업테스크_20260611.md  
> 브랜치: main (커밋 없음 — 작업 코드 존재, 미커밋 상태)

---

## 전체 요약

| 구분 | 상태 |
|------|------|
| 백엔드 스캐폴딩 | ✅ 완료 |
| 프론트엔드 메인 UI | ✅ 완료 |
| DB 모델 정의 | ✅ 완료 |
| Docker Compose 환경 | ✅ 완료 |
| 인증(JWT) API | ✅ 완료 |
| 음원 업로드 API | ✅ 완료 |
| 플레이리스트 CRUD | ✅ 완료 |
| 영상 렌더링(FFmpeg) | ✅ 구현 완료 (FFmpeg 미설치 시 RuntimeError) |
| 썸네일 생성(Pillow) | ✅ 완료 |
| 유튜브 업로드 | ⚠️ Mock 모드만 구현 (실 OAuth 미연결) |
| AI 메타데이터 생성 | ❌ 미구현 |
| 관리자 기능 | 🔧 라우터 파일 존재, 내용 미확인 |
| AWS S3 연동 | ❌ 로컬 파일 저장만 사용 중 |
| Redis/Celery 비동기 | ❌ docker-compose에 정의됨, 실 연동 미구현 |
| CI/CD | ❌ 미구현 |
| 테스트 | 🔧 test_api.py 파일 존재, 내용 미확인 |
| Git 커밋 | ❌ 커밋 없음 |

---

## 개발 환경

| 항목 | 상태 | 비고 |
|------|------|------|
| T-001 Git 레포지토리 초기화 | ✅ | 커밋 없음 |
| T-002 Next.js + Tailwind CSS | ✅ | `frontend/` 구조 완성 |
| T-003 FastAPI + Python | ✅ | `backend/` 구조 완성 |
| T-004 PostgreSQL 설치 및 스키마 | ⚠️ | docker-compose에 정의됨, 실제 앱은 **SQLite** 사용 중 |
| T-005 AWS S3 설정 | ❌ | 로컬 디렉토리 사용 중 (`uploads/`, `renders/`, `thumbnails/`) |
| T-006 Redis + Celery | ⚠️ | docker-compose에 정의됨, 앱에 실제 연동 안 됨 |
| T-007 Docker Compose | ✅ | `docker-compose.yml` 완성 (postgres, redis, backend, frontend) |
| T-008 CI/CD | ❌ | 미구현 |

---

## Phase 1 — 인증 및 기본 구조

| 태스크 | 상태 | 비고 |
|--------|------|------|
| T-101 users 테이블 | ✅ | SQLModel 모델 정의 완료 (id, email, password_hash, role) |
| T-102 POST /auth/register | ✅ | 구현 완료, 중복 이메일 체크 포함 |
| T-103 POST /auth/login (JWT) | ✅ | 구현 완료, bcrypt 검증 포함 |
| T-104 OAuth2 미들웨어 | ✅ | JWT 기반 인증 미들웨어 적용 |
| T-105 bcrypt 암호화 | ✅ | `services/auth.py` 구현 |
| T-106 레이아웃 컴포넌트 | ⚠️ | `page.tsx`에 인라인 구현, 별도 컴포넌트 분리 안 됨 |
| T-107 로그인/회원가입 페이지 | ❌ | 별도 로그인 페이지 없음 (API만 존재) |
| T-108 인증 상태 관리 | ❌ | 프론트엔드 인증 상태 관리 미구현 |
| T-109 API 클라이언트 설정 | ✅ | `frontend/app/api.ts` 구현 완료 |

---

## Phase 2 — MP3 업로드 기능

| 태스크 | 상태 | 비고 |
|--------|------|------|
| T-201 tracks 테이블 | ✅ | SQLModel 모델 정의 완료 |
| T-202 POST /tracks/upload | ✅ | 다중 파일 업로드 구현 |
| T-203 AWS S3 연동 | ❌ | 로컬 저장만 구현 |
| T-204 MP3 메타데이터 파싱 | ✅ | mutagen 라이브러리로 ID3 태그 파싱 |
| T-205 포맷 검증 | ✅ | MP3, WAV, FLAC, AAC, M4A 지원 |
| T-206 GET /tracks | ✅ | 구현 완료 |
| T-207 DELETE /tracks/{id} | ✅ | 구현 완료 |
| T-208 파일 업로드 UI | ✅ | Drag & Drop + 파일 선택 구현 |
| T-209 업로드 진행률 표시 | ⚠️ | busy 상태로 버튼 로딩 표시만, 진행률 바 없음 |
| T-210 업로드 파일 목록 | ✅ | 트랙 목록 테이블로 표시 |
| T-211 파일 삭제 기능 | ❌ | 프론트엔드에 삭제 버튼 미구현 (API는 있음) |

---

## Phase 3 — 플레이리스트 관리

| 태스크 | 상태 | 비고 |
|--------|------|------|
| T-301 playlists 테이블 | ✅ | 완료 |
| T-302 GET /playlists | ✅ | 완료 |
| T-303 POST /playlists | ✅ | 완료 |
| T-304 PUT /playlists/{id} | ✅ | 완료 |
| T-305 DELETE /playlists/{id} | ✅ | 완료 |
| T-306 플레이리스트 생성/편집 폼 | ✅ | 사이드바에서 생성 가능 |
| T-307 곡 순서 변경 (Drag & Drop) | ⚠️ | sort_order 필드 인라인 수동 입력 가능, Drag & Drop 미구현 |
| T-308 제목/아티스트/앨범 인라인 편집 | ✅ | 트랙 테이블에서 직접 편집 가능 |
| T-309 플레이리스트 목록 페이지 | ✅ | 사이드바에 목록 표시 |

---

## Phase 4 — 영상 생성 엔진

| 태스크 | 상태 | 비고 |
|--------|------|------|
| T-401 FFmpeg + MoviePy 환경 | ⚠️ | FFmpeg 명령어 직접 사용, MoviePy 미사용. **FFmpeg 미설치 시 RuntimeError** |
| T-402 음원 연결 Concatenate | ✅ | concat demuxer 방식 구현 |
| T-403 배경 이미지/영상 합성 | ⚠️ | 단색 배경만 지원 (`#12372a` 고정), 이미지/영상 배경 미구현 |
| T-404 자막(곡명) 오버레이 | ❌ | 자막 오버레이 미구현 |
| T-405 타임라인(챕터) 자동 계산 | ✅ | `chapters_for_tracks()` 구현 완료 |
| T-406 MP4 출력 및 저장 | ✅ | 로컬 `renders/` 디렉토리에 저장 (S3 미연결) |
| T-407 POST /video/render (Celery) | ⚠️ | 동기 처리로 구현됨, Celery 비동기 미적용 |
| T-408 GET /video/status/{id} | ✅ | 비디오 상태 조회 구현 |
| T-409 배경 이미지/영상 업로드 UI | ❌ | 미구현 |
| T-410 영상 생성 시작 + 상태 폴링 | ⚠️ | 시작 버튼은 있으나 폴링 없음 (동기 처리라 불필요) |
| T-411 생성 완료 후 미리보기/다운로드 | ✅ | 렌더 결과 링크 표시 |

---

## Phase 5 — 썸네일 생성

| 태스크 | 상태 | 비고 |
|--------|------|------|
| T-501 thumbnails 테이블 | ✅ | 완료 |
| T-502 AI 기반 배경 이미지 생성 | ❌ | Pillow 직접 생성만 구현, OpenAI/Gemini 미연결 |
| T-503 제목 텍스트 합성 | ✅ | Pillow로 텍스트 합성 구현 |
| T-504 썸네일 크기 조정 1280×720 | ✅ | 완료 |
| T-505 POST /thumbnail/generate | ✅ | 완료 |
| T-506 썸네일 스타일 템플릿 선택 UI | ⚠️ | `studio` 스타일 고정 (midnight, studio, forest 3종 존재, UI 미구현) |
| T-507 썸네일 미리보기 및 재생성 | ✅ | 출력 패널에 이미지 표시 |

---

## Phase 6 — 유튜브 업로드 연동

| 태스크 | 상태 | 비고 |
|--------|------|------|
| T-601 YouTube Data API v3 OAuth2 | ❌ | Mock 모드만 구현 |
| T-602 POST /youtube/connect | ⚠️ | 응답은 하지만 실 OAuth 플로우 없음 |
| T-603 POST /youtube/upload | ⚠️ | Mock ID 반환 (`mock-youtube-id-xxx`) |
| T-604 업로드 옵션 처리 | ✅ | 제목, 설명, 태그, 공개범위 처리 구조 완성 |
| T-605 예약 업로드 | ✅ | `publish_at` 필드 처리 완성 |
| T-606 플레이리스트 자동 생성 | ⚠️ | `POST /youtube/publish` 존재하지만 실제 유튜브 플레이리스트 생성 없음 |
| T-607 videos 테이블 | ✅ | 완료 |
| T-608 유튜브 계정 연결 버튼 | ❌ | UI에 OAuth 연결 버튼 없음 |
| T-609 업로드 설정 폼 | ✅ | 제목, 설명, 태그, 공개범위 UI 완성 |
| T-610 예약 날짜/시간 선택 UI | ✅ | datetime-local 입력 UI 완성 |
| T-611 업로드 진행 상태 표시 | ⚠️ | 버튼 로딩 표시만 (상세 진행률 없음) |

---

## Phase 7 — AI 메타데이터 생성

| 태스크 | 상태 | 비고 |
|--------|------|------|
| T-701 OpenAI / Gemini API 연동 | ❌ | 미구현 |
| T-702 영상 제목 자동 생성 | ❌ | 미구현 |
| T-703 영상 설명 자동 생성 | ❌ | 미구현 |
| T-704 SEO 태그 / 해시태그 자동 생성 | ❌ | 미구현 |
| T-705 챕터 타임스탬프 설명란 포맷 | ✅ | `chapters_for_tracks()`로 자동 생성 |
| T-706 메타데이터 수동 편집 UI | ✅ | 제목, 설명, 태그 직접 편집 가능 |

---

## Phase 8 — 콘텐츠 관리 및 관리자 기능

| 태스크 | 상태 | 비고 |
|--------|------|------|
| T-801 생성 영상 목록 페이지 | ❌ | 별도 페이지 없음, 현재 선택된 영상만 표시 |
| T-802 업로드/예약/실패 상태 표시 | ⚠️ | 현재 영상 status 텍스트만 표시 |
| T-803 실패 재시도 기능 | ❌ | 미구현 |
| T-804 회원 관리 | ❓ | `routers/admin.py` 내용 미확인 |
| T-805 업로드 모니터링 대시보드 | ❌ | 미구현 |
| T-806 통계 페이지 | ❌ | 미구현 |

---

## Phase 9 — 테스트 및 배포

| 태스크 | 상태 | 비고 |
|--------|------|------|
| T-901 단위 테스트 | ❓ | `backend/tests/test_api.py` 존재, 내용 미확인 |
| T-902 통합 테스트 | ❌ | 미구현 |
| T-903 E2E 테스트 | ❌ | 미구현 |
| T-904 대용량 처리 부하 테스트 | ❌ | 미구현 |
| T-905 4K 영상 생성 성능 테스트 | ❌ | 미구현 |
| T-906 AWS 인프라 구성 | ❌ | 미구현 |
| T-907 HTTPS 인증서 적용 | ❌ | 미구현 |
| T-908 환경변수/시크릿 관리 | ⚠️ | `.env.example` 존재, Secrets Manager 미적용 |
| T-909 프로덕션 배포 | ❌ | 미구현 |
| T-910 모니터링/로깅 | ❌ | 미구현 |

---

## 다음 우선 작업 (권고)

### 즉시 해야 할 것 (MVP 완성 필수)

1. **Git 최초 커밋** — 현재 작업 내용이 커밋 없이 워킹트리에만 존재
2. **YouTube OAuth2 실 연동** — 현재 Mock 모드, 서비스 핵심 기능
3. **프론트엔드 로그인/회원가입 페이지** — API는 있으나 UI 없음
4. **FFmpeg 설치 확인** — 영상 렌더링이 핵심 기능인데 환경 의존

### 이후 보완 사항

5. DB를 SQLite → PostgreSQL 전환 (프로덕션 대비)
6. 영상 렌더링 Celery 비동기 처리
7. 배경 이미지/영상 합성 (단색만 지원 중)
8. 자막 오버레이 구현
9. AI 메타데이터 생성 (OpenAI/Gemini 연동)
10. AWS S3 파일 저장 연동

---

## 기술 스택 현황

| 영역 | 계획 | 실제 구현 |
|------|------|----------|
| DB | PostgreSQL | **SQLite** (개발용) |
| 파일 저장 | AWS S3 | **로컬 디렉토리** |
| 영상 처리 | FFmpeg + MoviePy | **FFmpeg만** (동기) |
| 영상 처리 큐 | Redis + Celery | **동기 처리** |
| 썸네일 AI | OpenAI / Gemini | **Pillow** (로컬 생성) |
| YouTube | 실 OAuth2 | **Mock 모드** |
